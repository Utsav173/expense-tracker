<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Financial Statement - <%= accountName %>
  </title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
  <style>
    :root {
      --color-primary: #4f46e5;
      --color-primary-dark: #7c3aed;
      --color-text-header: #1e293b;
      --color-text-body: #374151;
      --color-text-muted: #64748b;
      --color-border: #e5e7eb;
      --color-background-light: #f8fafc;
      --accent-income: #10b981;
      --accent-expense: #ef4444;
      --accent-balance: #3b82f6;
      --accent-change: #8b5cf6;
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      -webkit-font-smoothing: antialiased;
      -moz-osx-font-smoothing: grayscale;
    }

    body {
      font-family: 'Inter', sans-serif;
      background: #f1f5f9;
      color: var(--color-text-body);
      line-height: 1.5;
    }

    .container {
      max-width: 8.27in;
      min-height: 11.69in;
      margin: 20px auto;
      background: white;
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
      display: flex;
      flex-direction: column;
    }

    .header {
      background: linear-gradient(135deg, var(--color-primary), var(--color-primary-dark));
      color: white;
      padding: 24px 32px;
      text-align: center;
    }

    .header h1 {
      font-size: 2rem;
      font-weight: 700;
      margin-bottom: 4px;
    }

    .header .subtitle {
      font-size: 1rem;
      opacity: 0.9;
      font-weight: 400;
    }

    .account-info {
      background: var(--color-background-light);
      padding: 20px 32px;
      border-bottom: 1px solid var(--color-border);
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
      gap: 16px;
    }

    .account-detail {
      display: flex;
      align-items: center;
      gap: 12px;
    }

    .account-detail .icon {
      width: 36px;
      height: 36px;
      background: linear-gradient(135deg, var(--color-primary), var(--color-primary-dark));
      border-radius: 8px;
      display: flex;
      align-items: center;
      justify-content: center;
      color: white;
      font-size: 1.2rem;
      flex-shrink: 0;
    }

    .account-detail .info h3 {
      font-size: 0.8rem;
      color: var(--color-text-muted);
      font-weight: 500;
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }

    .account-detail .info p {
      font-size: 1rem;
      font-weight: 600;
      color: var(--color-text-header);
    }

    .main-content {
      padding: 24px 32px;
      flex-grow: 1;
    }

    .analytics-header {
      text-align: center;
      margin-bottom: 24px;
    }

    .analytics-header h2 {
      font-size: 1.75rem;
      font-weight: 600;
      color: var(--color-text-header);
      margin-bottom: 4px;
    }

    .analytics-header p {
      color: var(--color-text-muted);
      font-size: 1rem;
      max-width: 600px;
      margin: 0 auto;
    }

    .analytics-grid {
      display: grid;
      grid-template-columns: repeat(4, 1fr);
      gap: 20px;
      margin-bottom: 32px;
    }

    .stat-card {
      border: 1px solid var(--color-border);
      border-top: 4px solid var(--card-accent);
      border-radius: 8px;
      padding: 16px;
      text-align: center;
    }

    .stat-card.income {
      --card-accent: var(--accent-income);
    }

    .stat-card.expense {
      --card-accent: var(--accent-expense);
    }

    .stat-card.balance {
      --card-accent: var(--accent-balance);
    }

    .stat-card.change {
      --card-accent: var(--accent-change);
    }

    .stat-card h3 {
      font-size: 0.75rem;
      color: var(--color-text-muted);
      font-weight: 500;
      margin-bottom: 8px;
      text-transform: uppercase;
    }

    .stat-card .amount {
      font-size: 1.5rem;
      font-weight: 600;
      line-height: 1.2;
    }

    .stat-card .currency {
      font-size: 0.8rem;
      color: var(--color-text-muted);
    }

    .transactions-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 16px;
      border-bottom: 2px solid var(--color-border);
      padding-bottom: 8px;
    }

    .transactions-header h2 {
      font-size: 1.5rem;
      font-weight: 600;
      color: var(--color-text-header);
    }

    .transaction-count {
      background: #eef2ff;
      color: var(--color-primary);
      padding: 4px 10px;
      border-radius: 16px;
      font-size: 0.8rem;
      font-weight: 600;
    }

    .table-container {
      width: 100%;
      border: 1px solid var(--color-border);
      border-radius: 8px;
      overflow: hidden;
    }

    table {
      width: 100%;
      border-collapse: collapse;
    }

    thead tr {
      background-color: var(--color-background-light);
    }

    th {
      padding: 10px 12px;
      text-align: left;
      font-weight: 600;
      color: var(--color-text-muted);
      font-size: 0.75rem;
      text-transform: uppercase;
      letter-spacing: 0.5px;
      border-bottom: 1px solid var(--color-border);
    }

    th:nth-child(1) {
      width: 12%;
    }

    th:nth-child(2) {
      width: 34%;
    }

    th:nth-child(3) {
      width: 18%;
    }

    th:nth-child(4),
    th:nth-child(5) {
      width: 12%;
      text-align: right;
    }

    th:nth-child(6) {
      width: 12%;
      text-align: center;
    }

    tbody tr:nth-of-type(even) {
      background-color: #f9fafb;
    }

    td {
      padding: 10px 12px;
      border-bottom: 1px solid #f3f4f6;
      font-size: 0.875rem;
      vertical-align: middle;
      word-break: break-word;
    }

    tbody tr:last-child td {
      border-bottom: none;
    }

    .transaction-text {
      font-weight: 500;
      color: var(--color-text-header);
    }

    .transaction-category {
      display: inline-block;
      background: #e0e7ff;
      color: #3730a3;
      padding: 4px 8px;
      border-radius: 6px;
      font-size: 0.75rem;
      font-weight: 500;
      line-height: 1.3;
      text-align: center;
      /* REMOVED max-width and text-overflow to allow wrapping */
    }

    .amount {
      font-family: 'Menlo', 'Monaco', monospace;
      font-weight: 600;
    }

    .income-amount {
      color: var(--accent-income);
    }

    .expense-amount {
      color: var(--accent-expense);
    }

    td:nth-child(4),
    td:nth-child(5) {
      text-align: right;
    }

    td:nth-child(6) {
      text-align: center;
    }

    .transfer-badge {
      background: #fef3c7;
      color: #92400e;
      padding: 4px 8px;
      border-radius: 6px;
      font-size: 0.75rem;
      font-weight: 500;
    }

    .no-transactions {
      text-align: center;
      padding: 40px;
      color: var(--color-text-muted);
    }

    /* --- Footer --- */
    .footer {
      background: var(--color-background-light);
      padding: 20px 32px;
      text-align: center;
      border-top: 1px solid var(--color-border);
      font-size: 0.8rem;
      color: var(--color-text-muted);
    }

    .footer p:last-child {
      font-size: 0.75rem;
      color: #94a3b8;
    }

    /* --- Print-specific styles --- */
    @media print {
      body {
        background: white;
        font-size: 10pt;
      }

      .container {
        box-shadow: none;
        margin: 0;
        max-width: 100%;
      }

      @page {
        size: A4;
        margin: 0.75in;
      }

      .analytics-grid {
        grid-template-columns: repeat(2, 1fr);
        /* 2x2 grid for printing */
      }

      tbody tr:nth-of-type(even) {
        background-color: #f9fafb;
        -webkit-print-color-adjust: exact;
        /* Ensures background color prints */
        print-color-adjust: exact;
      }
    }

    @media (max-width: 900px) {
      .analytics-grid {
        grid-template-columns: repeat(2, 1fr);
      }
    }

    @media (max-width: 600px) {
      .account-info {
        grid-template-columns: 1fr;
      }

      .analytics-grid {
        grid-template-columns: 1fr;
      }

      .main-content,
      .account-info,
      .header {
        padding: 16px;
      }
    }
  </style>
</head>

<body>
  <div class="container">
    <header class="header">
      <h1>Financial Statement</h1>
      <p class="subtitle">Comprehensive Account Overview</p>
    </header>

    <section class="account-info">
      <div class="account-detail">
        <div class="icon">🏦</div>
        <div class="info">
          <h3>Account Name</h3>
          <p>
            <%= accountName %>
          </p>
        </div>
      </div>
      <div class="account-detail">
        <div class="icon">💰</div>
        <div class="info">
          <h3>Currency</h3>
          <p>
            <%= accountCurrency %>
          </p>
        </div>
      </div>
      <div class="account-detail">
        <div class="icon">📅</div>
        <div class="info">
          <h3>Period</h3>
          <p>
            <%= dateRange %>
          </p>
        </div>
      </div>
    </section>

    <main class="main-content">
      <section class="analytics-section">
        <div class="analytics-header">
          <h2>Financial Overview</h2>
          <p>Summary of your financial activity for the selected period</p>
        </div>

        <div class="analytics-grid">
          <div class="stat-card income">
            <h3>Total Income</h3>
            <div class="amount income-amount">
              <%= (typeof totalIncome !=='undefined' ? totalIncome : 0).toLocaleString('en-IN', { minimumFractionDigits:
                2, maximumFractionDigits: 2 }) %>
            </div>
            <div class="currency">
              <%= accountCurrency %>
            </div>
          </div>

          <div class="stat-card expense">
            <h3>Total Expenses</h3>
            <div class="amount expense-amount">
              <%= (typeof totalExpense !=='undefined' ? totalExpense : 0).toLocaleString('en-IN', {
                minimumFractionDigits: 2, maximumFractionDigits: 2 }) %>
            </div>
            <div class="currency">
              <%= accountCurrency %>
            </div>
          </div>

          <div class="stat-card balance">
            <h3>Net Balance</h3>
            <div
              class="amount <%= (typeof balance !== 'undefined' && balance >= 0) ? 'income-amount' : 'expense-amount' %>">
              <%= (typeof balance !=='undefined' ? balance : 0).toLocaleString('en-IN', { minimumFractionDigits: 2,
                maximumFractionDigits: 2 }) %>
            </div>
            <div class="currency">
              <%= accountCurrency %>
            </div>
          </div>

          <div class="stat-card change">
            <h3>Income Change</h3>
            <div
              class="amount <%= (typeof incomePercentageChange !== 'undefined' && incomePercentageChange >= 0) ? 'income-amount' : 'expense-amount' %>">
              <%= (typeof incomePercentageChange !=='undefined' ? incomePercentageChange : 0).toFixed(1) %>%
            </div>
            <div class="currency">vs Previous Period</div>
          </div>
        </div>
      </section>

      <section class="transactions-section">
        <div class="transactions-header">
          <h2>Transaction History</h2>
          <div class="transaction-count">
            <%= (transactions && transactions.length) || 0 %> transactions
          </div>
        </div>

        <% if (transactions && transactions.length> 0) { %>
          <div class="table-container">
            <table>
              <thead>
                <tr>
                  <th>Date</th>
                  <th>Description</th>
                  <th>Category</th>
                  <th>Credit</th>
                  <th>Debit</th>
                  <th>Transfer</th>
                </tr>
              </thead>
              <tbody>
                <% transactions.forEach(transaction=> { %>
                  <tr>
                    <td>
                      <%= transaction.createdAt ? new Date(transaction.createdAt).toLocaleDateString('en-GB', {
                        month: 'short' , day: 'numeric' , year: 'numeric' }) : 'N/A' %>
                    </td>
                    <td>
                      <div class="transaction-text">
                        <%= transaction.text || 'No description' %>
                      </div>
                    </td>
                    <td>
                      <% if (transaction.category && transaction.category.name) { %>
                        <span class="transaction-category">
                          <%= transaction.category.name %>
                        </span>
                        <% } else { %>
                          <span style="color: #9ca3af;">Uncategorized</span>
                          <% } %>
                    </td>
                    <td>
                      <% if (transaction.isIncome) { %>
                        <span class="amount income-amount">
                          +<%= transaction.amount.toLocaleString('en-IN', { minimumFractionDigits: 2,
                            maximumFractionDigits: 2 }) %>
                        </span>
                        <% } %>
                    </td>
                    <td>
                      <% if (!transaction.isIncome) { %>
                        <span class="amount expense-amount">
                          -<%= transaction.amount.toLocaleString('en-IN', { minimumFractionDigits: 2,
                            maximumFractionDigits: 2 }) %>
                        </span>
                        <% } %>
                    </td>
                    <td>
                      <% if (transaction.transfer) { %>
                        <span class="transfer-badge">
                          <%= transaction.transfer %>
                        </span>
                        <% } %>
                    </td>
                  </tr>
                  <% }); %>
              </tbody>
            </table>
          </div>
          <% } else { %>
            <div class="no-transactions">
              <h3>No Transactions Found</h3>
              <p>There are no transactions to display for the selected period.</p>
            </div>
            <% } %>
      </section>
    </main>

    <footer class="footer">
      <p>This statement was generated on your request.</p>
      <p>Generated on <%= typeof generatedAt !=='undefined' ? generatedAt : new Date().toLocaleString() %>
      </p>
    </footer>
  </div>
</body>

</html>